{% extends "/email/baseEmail.twig" %}

{% block contingut %}
    <h1>Validacions d'alumnes pendents</h1>
    {% if alumne %}
        <p>Hi ha una petició de validació d'algun alumne del centre d'uns estudis que no tenen cap professor assignat.</p>
        <p>El cicle és {{ alumne.estudisAlta }} - {{ cicle.nom }}</p>
    {% else %}
    <p>Té una nova petició de validació d'algun alumne del centre.</p>
    <p>Per favor, entri a l'aplicació i comprovi-ho el més aviat possible.</p>
        <p><a style="background-color: #3c763d; color: #ffffff; padding: 14px 25px;text-align: center; text-decoration: none; display: inline-block;" class="boto" href="{{ ruta }}/valAlumnes?t={{ token }}">Validar alumnes</a></p>
        <p>Aquest enllaç té una validesa de set dies.</p>
        <p></p>
        <p>Si no veu correctament el botó pot validar l'oferta clicant el següent enllaç: <a href="{{ ruta }}/valAlumnes?t={{ token }}">{{ ruta }}/valAlumnes?t={{ token }}</a>. </p>
        <p></p>
    {% endif %}
        <p>Moltes gràcies!</p>

{%endblock%}
